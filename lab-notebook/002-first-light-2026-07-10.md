# 002 — Pairing, troubleshooting, first light

**Date:** 2026-07-10 evening (payload timestamps 2026-07-11 UTC).
Continues [001](001-unboxing-2026-07-10.md).

## Pairing & firmware

- Paired to iPhone; immediate OTA firmware update **v3.2.12 → v3.3.4**.
  Study baseline is v3.3.4.
- Post-update the BLE bond broke silently: app alive, pendant advertising,
  single-click = 3 blinks (disconnected), app's "Start Transcribing" button
  silently did nothing. **Fix per vendor troubleshooting doc [verified]:**
  forget device in iOS Bluetooth settings, re-pair via app.
- Time lost ≈ 40 min. A silent no-op button on a disconnected device is a
  UX bug worth reporting to the vendor.

## Docs-vs-device contradictions collected today

| Item | Device guide | LED help article | Observed |
|---|---|---|---|
| Capture start | "works automatically — just talk" | — | App requires manual "Start Transcribing" (or button hold per in-app overlay) |
| Power hold | 2 s | 3 s | not timed yet |
| Green LED | battery 80%+ | battery >15% | green shown on charger |
| Steady white | "connected and operational" | "connected and transcribing in real-time" | — |
| Button hold | power on/off only | power on/off only | in-app overlay: "Hold the Button to Start & Stop Recording" |

## First scripted take (B-quiet, take "one" — see note)

- Procedure: app Start Transcribing → slate + Harvard set 1 → manual
  end-conversation in app. Speech ran 71 s. Researcher-only, quiet room.
- **Webhook pipeline worked end-to-end on the first try**: single POST to
  the funnel URL, token accepted, archived to ZFS. Sample (sanitized):
  [`../data/samples/payload-001-first-light.json`](../data/samples/payload-001-first-light.json);
  schema write-up: [`../data/webhook-schema.md`](../data/webhook-schema.md).
- **RQ3**: all 10 Harvard sentences word-perfect (WER ≈ 0). Excellent
  quiet-room baseline.
- **RQ1**: no conversation ID / event type / wearer marker in payload.
- **RQ6 anomaly**: segment `timestamp` fields postdate payload receipt by up
  to ~53 s → synthesized, not measured. Details in the schema doc.
- Slate discrepancy: transcript reads "session B, quiet one"; the planned
  slate was "quiet two" (take one was spoken before transcription was
  active and produced no data). Whether the researcher said "one" or the
  ASR erred is recorded as unresolved; the take is logged as the sole
  B-quiet capture regardless.

## Battery log

- Arrived depleted (would not power on). Charged from ~18:30 local.
- 20:52 local: app showed device battery pill red "0" (later green LED on
  charger = >15% per LED article). Formal rundown clock starts at first
  confirmed full charge.

## Open from today

- Confirm the recessed back opening is USB-C (assumed — the included cable
  charged it, but the connection wasn't photographed).
- Developer Settings walk + API key + MCP connection (checklist §3 partially
  done: webhook URL configured ✓).
- LED during active transcription: expected steady white — not yet observed/
  photographed.
- App-transcript vs webhook-payload diff for the same conversation.
